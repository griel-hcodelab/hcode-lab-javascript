//Undefined = Algo que não foi definido, por exemplo varável declarada mas não definida
//Null = Algo que não existe

let pessoa;
console.log(pessoa); //A variável existe mas não contém valor

let nome = null;
console.log(nome); //A variavel existe mas o conteúdo dentro dela não existe
