/**
 * Criar um package.json de forma simplificada: npm init -y
 * Instalar o servidor Express: npm install express
 * Rodar o servidor: node ARQUIVO.JS (Como produção)
 *
 * Nodemon: Reinicia o servidor sempre que há alguma alteração
 * npm install nodemon --save-dev
 *
 * Instando o NeDB, banco de dados simples e leve
 * npm install nedb
 */
